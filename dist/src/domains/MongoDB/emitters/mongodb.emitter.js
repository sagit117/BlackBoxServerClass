import EventEmitter from "event-emitters-class";
export default class MongodbEmitter extends EventEmitter {
    constructor(proto) {
        console.log("created MongodbEmitter");
        super(proto);
    }
}
//# sourceMappingURL=mongodb.emitter.js.map