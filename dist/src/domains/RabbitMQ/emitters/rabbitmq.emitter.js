import EventEmitter from "event-emitters-class";
export default class RabbitmqEmitter extends EventEmitter {
    constructor(proto) {
        console.log("created RabbitmqEmitter");
        super(proto);
    }
}
//# sourceMappingURL=rabbitmq.emitter.js.map