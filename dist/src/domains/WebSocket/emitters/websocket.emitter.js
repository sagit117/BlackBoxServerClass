import EventEmitter from "event-emitters-class";
export default class WebsocketEmitter extends EventEmitter {
    constructor(proto) {
        console.log("created WebsocketEmitter");
        super(proto);
    }
}
//# sourceMappingURL=websocket.emitter.js.map